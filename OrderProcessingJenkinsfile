pipeline {
	agent none

	environment {
		DOCKER_HOST_IP = '192.168.1.56'
		NETWORK = 'order-processing-net'
	    }
stages {
	stage('Docker Network') {
		agent any
		steps {
			sh 'docker network create $NETWORK'
		}
	}
        
  }
}

